# Spec file for building the looking-glass kvmfr module as a kmod inside copr
Primarily meant to be used when making Fedora Atomic Desktop images as the kernel module has to be included with the image.
